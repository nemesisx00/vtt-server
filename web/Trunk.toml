[build]
dist = "../target/web"
public_url = "/web/"
release = true

[[hooks]]
stage = "pre_build"
command = "powershell"
command_arguments = ["npm", "run", "stylus"]

# [[hooks]]
# stage = "pre_build"
# command = "sh"
# command_arguments = ["-c", "npm", "run", "stylus"]
